# 历史验收输入

这些真实旧档和故障夹具清单是当前测试或浏览器验证的输入，保留归档原件字节。原路径、来源基线及校验值见[历史证据索引](../../../docs/verification/historical-evidence-files.md)。不要重新排版、重新导出或用当前规则生成这些历史输入。

`prepare-acceptance-fixtures.ts` 在 ignored `test-results/` 中复现旧故障夹具；它输出的历史清单继续使用原路径作为来源元数据，读取输入则解析到本目录。
