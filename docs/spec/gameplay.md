# 玩法与机关合同

[文档索引](../index.md) · [主规格](overview.md) · [内容数据](content.md) · [工程与存档](architecture.md) · [验收](acceptance.md)

> 当前实施状态：2026-09-11 已批准基线并进入 M1–M5 连续实施。本文尚未实现等表述保留规格交付时的历史语境；当前范围、实际验证与限制统一见[实施进度](../verification/implementation-progress.md)，不由合同文字推定验收通过。

本文全部精确规则和参数为 [设计]。原活动机制的有限依据见 [来源记录](../references/index.md)，不能将本表当作原版算法。凡成功涉及全局目标 / 奖励，必须走同一个规则事务。

## 1. 棋盘、可见性与占格

每张地图使用整数 `(x, y)`；x 向右、y 向下。仅四向相邻，无斜走、跳格或拉物体。空缺坐标视为地图外，不能用矩形宽高补出可走格。地形与实体分开：一个格子可同时有地板、接收槽、可移动对象；至多一个阻挡实体。

中心和主地图移动规则相同。主地图的房间以显式边界划分；房间不是独立菜单关卡，进入 / 离开仍从地图上的入口 / 出口发生。终端小游戏使用独立局部棋盘，保存外层入口位置。

| 格子状态 | 表现 | 可操作性 |
| --- | --- | --- |
| 未发现 | 黑暗背景或无内容的电视轮廓，不展示图标 | 不能远程寻路或点出隐藏内容 |
| 已发现、以太覆盖 | 可辨识覆盖边框，内容图标隐藏 | 不可走入，等待对应富集节点清除 |
| 已发现、清除、未访问 | 展示已知地形和对象 | 手动相邻移动；自动寻路不穿过未访问格 |
| 已访问安全格 | 正常屏幕 | 满足安全条件时可用于自动行走 |
| 隐藏路径 | 初始不可见 | 仅由指定观察点 / 增幅仪 / 按钮揭示，不能靠射线拾取泄露 |

进入普通格后显露当前格及其四个相邻格，不向第二圈传播，也不穿透显式隐藏组。显露以太覆盖格不等于驱散覆盖；增幅仪清除覆盖不自动“访问”它们。发现记录通常永久保留；迷宫的危险分布只在观察阶段显示，不写入普通地图图例。

## 2. 输入合同

画布获得游戏焦点时消费游戏按键，方向键不滚动网页；输入框、菜单按钮和导入对话框获得焦点时不发送移动命令。所有功能都有中文文字按钮或明确图标提示，键盘和鼠标经过同一合法性判断。

| 输入 | 普通探索 / 静态解谜 | 特殊模式 |
| --- | --- | --- |
| WASD / 方向键 | 四方向一步一格 | 防火墙为单次按键移动；杀毒 / 幽灵可长按移动 |
| 点击相邻格 | 一次相邻移动或推物尝试 | 各挑战按下文覆盖 |
| 点击远格 | 仅普通探索可发起已访问安全路径 | 静态谜题 / 幽灵只接受相邻点击；防火墙 / 杀毒允许局部棋盘直接跳到目标 |
| F / 交互按钮 | 当前格终端、开关、观察点、挑战入口；当前格无对象时提示 | 杀毒可清除光标所在格目标；其他实时挑战不复用 F |
| R / 增幅按钮 | 玩家站在有效富集节点上使用 | 所有挑战内禁用 |
| Z / 撤销 | 仅静态房间的活动阶段 | 实时模式禁用 |
| 重置房间 | 静态房间入口布局重开 | 实时模式为重新挑战 |
| M / 区域图 | 只显示已发现地图及已激活传送点 | 挑战中仅提供暂停菜单，不允许通过传送逃避局部结算 |
| Esc / 暂停 | 设置、保存退出、返回入口 | 实时模式冻结有效时间；恢复需显式操作 |

普通移动动画初值 140ms。长按由输入适配器自行处理：首次重复等待 250ms，此后每 140ms 尝试一次，忽略浏览器自身 `keydown.repeat`。同一时刻按多方向时只使用最近按下且仍按住的方向；松开后回到前一个仍按住的方向，不能生成斜向命令。

普通探索 / 静态模式最多保留 1 个等待方向；新方向替换旧方向。动画只约束显示与接受下一操作的节奏，不决定成功事件。无效移动不入撤销栈、不存档、不积累补偿步数。模式切换、暂停、失焦、地图传送、失败、重置、导入和加载时清空按键及命令队列，恢复要求松开后重新按下。

鼠标按 `pointerdown` 只生成一个动作，不再对同一手势的 `click` 生成第二个动作。菜单覆盖时禁止画布命中；点击过期对象按无目标处理。防火墙与杀毒的局部直接点击例外须在入场说明中写明，不能外溢为主地图瞬移。

### 2.1 自动行走

仅在 `explore` 中执行最短安全路径；等长路线按上、右、下、左选取，保持重放一致。候选格必须已访问、已发现、无覆盖、可通行，且不存在未领取奖励、交互对象、危险、挑战入口、推物或传送出口。只走普通地板及已打开且无其他事件的门。

路径逐步重新校验，不能一次提交最终坐标。新键盘方向 / 新点击取消旧路径；遇见边界变化、模式入口或未清理事件时停在其前一个安全格，由玩家继续操作。不跨地图寻路，不利用隐藏地形计算路径，不自动求解静态谜题。

## 3. 单次行动与事务

规则层接收命令、内容和权威状态，返回新状态、结果码与反馈事件。顺序固定如下：

1. 检查模式、暂停、输入对象、边界、已知权限和占格。无效返回具体原因，不能部分修改布局。
2. 求本次位移及推物完整落点；所有受影响对象一起检查。长距离滑球是一次原子动作。
3. 暂存位置 / 局部状态，做该模式的危险与失败判定。失败直接转入房间恢复，不执行该动作的拾取和成功事件。
4. 执行当前格的自动事件：普通数据 / 物资拾取、首次到达传送点、房间内灯。需 F / R 的终端不在踩踏时隐式激活。
5. 更新显露、解谜完成条件、目标结果；计算派生区域进度、下一区开放、回访门及仓库门。
6. 若是未完成静态房间，保留临时结果；若到达房间成功条件，整包提交其目标、奖励、门及完成布局。普通探索的拾取直接全局提交。
7. 递增权威修订号，生成不可重复的反馈事件，发出稳定快照；渲染 / 声音订阅结果。保存失败只影响持久化状态，不把成功判定重做一次。

一次操作最多跨一个场景或进入一个挑战；传送落点不得立即触发反向传送，须先离开再重入 / 交互。内容不允许无限链式传送或递归触发器。所有永久门为开启后锁存；需要反复变化的物体 / 门只能存在于房间局部状态中。

同一成功事务既完成目标又登记已领取奖励；外层 UI 不再执行“发奖励”。拿过的奖励 ID 再触发时返回“已领取”，总量始终从 ID 集合求和。读取旧快照会整体回到旧世界和旧领取集合，不在旧总数上再次累加。

## 4. 状态与时间

| 运行模式 | 允许的推进 | 时间规则 | 正常退出 |
| --- | --- | --- | --- |
| `explore` | 普通地图行动 | 不随等待变化，无总倒计时 | 入口 / 终端 / 传送 / 菜单 |
| `staticPuzzle`，`preview` 阶段 | 记忆迷宫观察，禁止移动 | 观察有效时间 4,000ms | 自动转 `active`，或菜单退出 |
| `staticPuzzle`，`active` 阶段 | 迷宫、一笔画、推物 | 每次合法行动更新，无墙钟消耗 | 成功、重置、放弃 |
| `challengeReady` | 说明、难度选择 | 尚未启动时钟 | 点击开始后 3 秒准备倒数 |
| `challengeRunning` | 防火墙 / 杀毒 / 幽灵 | 专用有效时钟 | 成功、到时、碰撞、放弃 |
| `challengeResult` | 结果、重试、返回 | 停止 | 重试入准备态，返回外层入口 |
| `transition` | 已提交的区域切换动画 | 不接受玩法输入 | 新场景显示完毕回探索 |

暂停是覆盖在当前模式上的状态，有 `manual / hidden / blur / clockGap / graphicsLost` 原因集合。存在任一原因时不处理玩法命令；自动暂停同时设置等待玩家恢复的标记。窗口重新可见 / 获得焦点只移除相应系统原因，仍展示“继续”，不会自行开始计时。只有页面可见、画布已可操作且图形可用时，Resume 才清除等待标记及 manual / clockGap；尚未消失的系统原因不能被强制清除。静态模式恢复立即可操作；实时模式恢复有 3 秒提示倒数，倒数不计入挑战时间；迷宫观察按剩余观察时间恢复。

### 4.1 有效时钟

运行时以单调性能时钟计毫秒，扣除暂停与准备倒数；不使用系统日期、帧数或 `setInterval` 调用次数。实时事件按有效时间处理；测试注入同一时间源。浏览器后台会节流或暂停动画回调，因此须显式处理可见性变化。[性能时钟](https://developer.mozilla.org/en-US/docs/Web/API/Performance/now)、[页面可见性](https://developer.mozilla.org/en-US/docs/Web/API/Page_Visibility_API)

输入适配器在事件处理入口记录单调时间与递增序号。推进到时间 t 时，先处理 t 之前的全部计划事件，再按下文规则处理 t 的事件。同一时间多输入按序号；一个事件不能因多次渲染重复执行。

连续两次前台调度间隔超过 250ms 时，在推进之前进入 `clockGap` 暂停，冻结到最后已处理时刻，清空该间隔的新输入；不追补大量幽灵步数或判为错拍。暂停与卡顿不扣奖励，不把恢复倒数视为游戏时间。帧率正常但低于目标时仍按时间表运行。

防火墙 / 杀毒有效区间为 `[0, durationMs)`。到截止时刻先结束挑战，再处理同刻输入；输入恰好在终点不计分。杀毒同刻顺序为目标过期 → 新目标出现 → 输入 → 成绩派生；幽灵采用其专用同步碰撞规则。静态关卡没有超时。

### 4.2 失败、放弃、撤销和重放

每个房间定义安全外层入口、内部出生点、重置布局、成功出口。入口不能是危险格或自动触发回跳的传送格。失败 / 放弃丢弃本次临时数据，保留之前全局进度，无生命 / 货币损失。

静态活动阶段的每次合法动作留下完整前态，包含玩家、物体、已走路径、局部灯 / 机关和待发结果；最多保留最近 256 步。没有撤销历史时显示不可撤销，仍能重置。撤销不倒退其他房间已提交的永久状态；成功是边界，提交后清空历史，不能撤销领奖。刷新后恢复布局但不恢复撤销历史，提示可重置。

房间重置回本次入口基线，记忆迷宫重新观察；放弃回外层入口。静态房间成功后将玩家置于安全成功出口，退出 active 模式；已成功房间保持可通行的完成布局，迷宫危险 / 一笔画重复访问限制不再作用，推物停在成功布局且不接受新的推动。重新挑战使用独立练习实例，不会关闭永久门或撤回权限。实时挑战每次从零开始，本轮中间 Combo / 分数 / 幽灵灯态不持久化。

## 5. 公共机关

| 类型 | 操作与前置 | 成功结果 | 重复 / 失败 |
| --- | --- | --- | --- |
| 阳炎增幅仪 / 富集节点 | 站在节点按 R；具备能力；节点已显露 | 只清除该节点 `clearsTileIds`，显露指定组，提交列出的目标 / 奖励 | 无效地点提示；已用节点无效果，不计次数；中心第一次同时授予无限能力 |
| 数据芯片 | 踏入可达数据格 | 完成固定 `objectiveId` | 不可重复加权，不占可消费背包 |
| 按钮 / 清理终端 | 在对象格按 F | 开启所列门、显露组或完成固定目标 | 已激活保持开启；禁止把战斗参数留作前置 |
| 条件门 | 条件真后自动永久开启；撞关闭门展示原因 | 改变通行和外观 | 读取时从目标重算；不从动画完成回调开门 |
| 观察点 | 站立按 F | 显露指定隐藏路径组，或开始迷宫预览 | 可重复观察；无额外物资 |
| 物资格 | 踏入；需门禁的先开门 | 登记奖励 ID，按整数单位显示 `+5` 等 | 显示空箱；不能刷取 |
| 传送点 | 首次到达激活；F 打开目标选择，或 M 选择已激活点 | 到目标安全落点，沿用世界状态 | 未激活 / 未收录不可选；挑战中不能直接传送 |
| 挑战访问终端 | 已到达且本版收录，F | 开放该组档位 | 本版三个正式难度互不依赖，不要求 NPC / 前一档胜利 |

## 6. A 区：记忆迷宫与防火墙

### 6.1 记忆迷宫

固定内容为 `a.maze.01 / 02 / 03`。01 位于主路径，02 / 03 属开库数据支路。进入后展示安全 / 危险格分布 4 秒，随后隐藏危险图标；布局不随机，不限尝试次数。初次说明只解释目标，不提前展示解答序列。

玩家以相邻移动到出口即成功；危险格触发失败，本次所有暂存结果丢弃，回内部出生点重新观察。墙 / 地图外属于无效动作，不算踩错。再次观察等价于重置尝试，不允许站在迷宫中段随时显示答案；不存在根据点击目标自动算出安全路径的功能。

成功按实际地形与出口判定；允许任何合法路线，不比较预录按键串。每房至少提供一条通关见证，01 在不取得任何后期能力时可解。见证用于校验，不能成为运行时答案判定。

### 6.2 突破防火墙

外层终端进入局部 **5 × 4** 方格，光标从中间位置 `(2,2)` 开始。2026-09-11 按用户要求核对原版后采用该构图，并增加左右四块 COMBO 电视。110 BPM 来自三首原曲公开试听的实测；精确游戏谱面、首拍和判定窗仍为重建参数，依据与边界见 [S11](../references/firewall-audio.md)。方向键只能相邻移动，鼠标可直接选择局部任意不同格；点当前格 / 越界不移动也不计分。禁止按住键自动产生节拍输入，每次物理按下只判一次。

| 档位 | 有效时长 | BPM / 拍间隔 | 拍数 | 通过所需最高 Combo |
| --- | --- | --- | --- | --- |
| 教学 | 15 秒 | 110 / 60000÷110 ms | 27 | 12 |
| 内层 | 45 秒 | 110 / 60000÷110 ms | 82 | 40 |
| 深层 | 45 秒 | 110 / 60000÷110 ms | 82 | 55 |
| 核心 | 45 秒 | 110 / 60000÷110 ms | 82 | 70 |

拍点从半拍 `30000÷110 ms`（约 272.727ms）开始，每隔一拍（约 545.455ms），最后一拍约为 14,454.545 / 44,454.545ms；判定窗为拍点前后各 150ms，两个端点计入有效。计算保留完整精度，不逐拍四舍五入。窗口互不重叠，第一拍和最后一拍的完整窗口均落在挑战内。教学完成开放正式档位访问；三档之间可自由选择。

配乐、周边白光、底部“现在移动 / 本拍命中”文字和成绩读取同一有效时间；成功命中时侧屏短暂显示 PERFECT，错拍有效移动显示 MISS。暂停和失焦停止配乐，恢复倒数结束后从冻结的乐句位置续播。三首随包配乐为 110 BPM / 32 拍原创替代循环，不包含原曲录音。减少闪烁关闭白光及亮暗变化，保留固定底色的文字和危险格符号；静音仍可完整跟拍。

一次移动只匹配最近拍点。有效窗内移动：该拍尚未记分则当前 Combo 加 1；该拍已经记分则移动但不加分、不额外罚分。错拍移动到普通格减 5，最低 0；错拍移入危险格清零。有效拍点移入危险格允许通过并正常计分。没有移动的拍点不加分也不罚分；此处 Combo 是本版可扣减计数，不是严格的连续命中拍数。

危险图案由固定 `beatMasks` 提供，每拍一项，连续四拍保持一组行列图案，最多覆盖教学 0、内层 5、深层 8、核心 12 格；同一难度每次重试一致。每项在其拍点前半拍起生效，到下一项生效时替换，末项保留到结算；新增危险格提前 150ms 显示“!”预告。只在移入时检查，站立处变危险不会被动扣分。谱面必须保证每个拍点都有至少一个不同于当前格的可选落点；无额外墙。未测绘部分采用可审阅的重建谱面，不称原版图案。

最高 Combo 随当前值更新，到时按最高值判定成功；达标后可继续提高成绩，不重复发奖励。教学不发物资，正式三档各解锁一个 5 单位物资格。计分挑战不提供开库数据。每拍最多 +1 的设计上限是 27 / 82，所有阈值必须低于该上限并有真实时间操作记录。v1 的 5×5 / 120 BPM / 30或90拍保留在历史内容源与证据中；v2 将旧成绩归档，已完成目标、合法奖励和主线进度保留。

### 6.3 A 回访

`d.main` 后回访门开启。支路保留富集节点、绕行和末端终端关系；末端交互给 `a.data.revisit`，独立物资格给 5 单位。到达终端不要求 A 已有 80 数据，途中缺的首访数据可以之后补。

## 7. B 区：一笔画与杀毒

### 7.1 一笔画

`b.line.01 / 02` 各有固定起点、终点和必经格集合。起点算已访问 1 次；只允许相邻走到尚未访问的必经格。目标是每个必经格恰好访问一次，最后到达终点。终点属于必经集合，在其他格未全访问前禁止进入并说明原因。

墙、非路径、重复访问属于无效动作，不立即失败；玩家可撤销或重置解决断路。没有自动计时或生成答案。01 是 B 主路径前置；02 完成后给 `b.data.line02`。两者各有一个 5 单位物资格，奖励与数据不是同一字段。

### 7.2 杀毒程序

局部棋盘为 **5 × 5**，光标初始在中心格。方向键移动到目标格即清除；F 清除当前格目标；点击任一活跃目标可瞬移并清除，点击空格只移动。生成在光标脚下不会自动清除，仍需一次输入。每个目标有唯一实例 ID，已清除 / 已过期的 ID 再次点击不计分。

| 档位 | 时长 | 生成间隔 | 通过分数 |
| --- | --- | --- | --- |
| 轻度 | 45 秒 | 700ms | 40 |
| 中度 | 45 秒 | 500ms | 60 |
| 重度 | 45 秒 | 350ms | 80 |

蓝色方块 1 分，寿命 1,800ms；紫色菱形 2 分，寿命 1,000ms；金色星形寿命 1,800ms，本体 0 分。星星被清除时，在同一事务清除当时所有活跃的蓝 / 紫目标，各按正常分值加分；不触发其他星星，不影响未来或已过期目标。同帧点击与星星覆盖同一个目标仍只得一次分。

为了使首套内容可制作，生成脚本以 k 从 0 起编号，在 `500 + k × 间隔` 毫秒生成，时间须小于 45,000ms。k 除以 10 余 9 时为星星，否则 k 除以 4 余 3 时为紫色，其他为蓝色。候选格按行优先编号 0–24，以 `7k` 除以 25 的余数为起点，循环找第一个与已排定目标生命周期不重叠的格。**此排表在内容制作时一次生成并冻结为数据**，不根据玩家清除速度改变；校验器拒绝同格重叠排期。

目标活跃区间为出现时刻含端点、过期时刻不含端点；截止时清除全部剩余显示。漏掉目标不扣分。到时按总分判定，每档首次成功开放各自 5 单位物资格；最佳分只取最大值。星星一次清除数量另记录本局与最好值，但不作为数据或奖励额外前置。

自动验证须计算该固定脚本所有非星目标分值总和，确认不低于门槛，并用“看见后至少 150ms 再操作”的见证通过。仅有理论总分足够不能证明该脚本可玩。

### 7.3 B 回访

`d.main` 后从已开放 B 主图进入回访支路，清除对应覆盖并到达终端取得 `b.data.revisit`；支路物资 5 单位。与 A 回访无顺序依赖。

## 8. C 区：滑球、推车与数据盗取

### 8.1 主地图组合机关 `c.routing.01`

该机关有显式房间边界，内部同时包含滑行信号球、逐格推车及目标基站。玩家走向球时，球沿方向滑行到下一步将越界 / 撞墙 / 撞阻挡实体的位置，或进入一个基站格立即停止；玩家只进入球的原格，不跟随球滑行。

推车被推动时只移动 1 格，玩家进入车的原格。不可拉取、串推、越界或穿过其他球 / 车。没有空间使对象移动时整条命令无效。球滑行途中的格子不触发玩家拾取 / 开关；只有球最后落点参与目标匹配。

基站是可通行地板层，任何球进入都会停下；只有 `targetStationId` 匹配才满足该球目标。错误基站不会销毁球，可继续推动。成功前正确入站的球也可被推离，条件随布局重算。所有指定球处于指定基站时一次成功；车仅作为阻挡，不要求停在虚构的目标点。

机关支持完整撤销与重置；重置同时还原球、车、玩家及本次暂存效果。成功打开通往 C 主终端的门，永久进度在成功时才提交。10 单位主图物资分为两个 5 单位领取记录，放在机关前后已证明可达的收集路径，不能通过重置重复获得。

### 8.2 数据盗取 `c.theft.01 / 02 / 03`

终端打开局部格子谜题，棋盘尺寸 / 墙由各关固定数据给出。玩家推动彩色对象 **一次一格**；不能调用主地图滑球规则，不能拉取或串推。每个对象有一个匹配颜色的接收槽，颜色同时用形状 / 标记区分。

对象推到异色槽仍占格，可再推走，但不算成功；正确槽上的对象在整体成功前也不锁死。玩家可经过空槽。所有对象占据匹配颜色槽且全部必需槽被填满时成功；同色对象允许互换，不按对象 ID 强制唯一解。每关对象数 = 必需槽数；布局不得要求以额外对象占住无关槽才能“算满”。

失败走法由撤销 / 重置恢复，无时间限制。每关成功分别给 20 数据并开放 5 单位物资领取点。这三关属于开库必需内容，但不是开放 D 的条件。

## 9. D 区：隐藏路线、幽灵与权限

D 有 4 条权限路线和一条隐藏数据支路。权限终端在对应路线尽头 F 交互，四权限分别给 20 数据并自动派生 `d.main`。问答、角色知识和剧情密码不参与判断。

隐藏道路只由有图标 / 提示的观察点、灯或富集节点揭示。必需权限路线的起始线索必须可达，不允许要求玩家盲点全屏寻找不可见入口。隐藏数据另占 20 权重，可以在 D 主路径后回来补。

### 9.1 幽灵段

每段为局部实时房间，有安全内部出生点、明确出口、灯分组与幽灵固定路径。进入说明后主动开始；前 3 秒准备倒数显示全部可见危险位置。没有总超时或追逐 AI。

幽灵每 500ms 沿有序路径前进 1 格，路径只能四向连续；采用首尾也相邻的循环路线，首步在有效时间 500ms，初始相位为路径第 0 格。不同幽灵使用各自路径，房间数据不允许它们在同刻重合或交换；不存在随机速度。玩家移动的最短间隔为 140ms，与普通长按节奏一致，不由幽灵步频决定。

当玩家与幽灵事件同刻发生，先计算双方的移动意图，再统一检查：最终同格，或双方在此时交换原格，均为碰撞；幽灵走入静止玩家也碰撞。仅仅同时跟随离开同一格、且无同格或交换，不碰撞。连续输入同刻最多处理一个玩家移动，多余移动丢弃。

碰撞判定优先于同刻灯的触发；存活后踏入灯格即自动点亮，移除其 `ghostIds` 内的幽灵，其他组保持不变。不能利用同刻亮灯穿过幽灵。灯效果只在本次尝试中有效，碰撞 / 重试恢复所有初始幽灵和灯。

达到段出口即成功，提交该段完成状态，使其后普通回访成为安全通路；需要重复挑战时进入练习实例。权限 / 永久物资放在段出口安全侧，不能在危险段中边领永久奖励边重置。碰撞回内部入口并显示重试 / 返回，重试前时钟保持停止。

每段要有真实等待与移动组成的通关见证，覆盖至少一次亮灯；不能仅证明所有灯瞬间已开时出口可达。不同段的完成互不撤销。

## 10. 中央仓库与结算

四区完整数据集合齐全后，中心仓库门在同一事务打开，地图提示新目标。仓库保留从入口到最终终端的固定收集路线，无战斗实例、敌人属性或首领失败分支。

最终终端 F 交互时同时完成 `warehouse.complete` 和 `hub.supply.final`（9 单位），显示本次主目标完成、各区数据、物资已收 / 总量与未完成可选挑战。结算后可直接返回地图；最终终端重复交互只展示结果，不重复领取。

物资达到 130 与主目标完成是两个独立结果；完成一者不会冒充完成另一者。所有挑战可继续练习，最好成绩可提高，永久奖励最多一次。
