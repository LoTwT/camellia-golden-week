# PR 数据精简

[文档索引](../index.md) · [历史证据索引](historical-evidence-files.md) · [R1 原始证据归档](r1-evidence-archive.md)

本轮覆盖 PR #1 相对 main 的完整差异。开始时 HEAD 为 `ba82f902e34365c9a38a179b0d6efc6b75faf371`，GitHub 与本地均为新增 1,375,244 行、删除 196 行。其中历史及当前证据 744,820 行、冻结内容 244,297 行、世界路线数据 298,260 行，其余 87,867 行。上轮仅整理最近两笔 R1 提交，传输体积下降不等于整个 PR 已精简。

## 原始证据与必要输入

853 个历史 M1–M5、防火墙及菜单验收文件共 98,054,063 字节，原路径和原字节归档到 `historical-evidence.zip`。17 份当前测试或浏览器验证仍读取的真实旧档及故障夹具清单共 121,887 字节，保留在 `tests/fixtures/historical/`。当前 R1 的代表截图、原摘要和已发布归档清单继续保留。

归档文件名、大小及 SHA-256 由[逐文件清单](https://github.com/LoTwT/camellia-golden-week/releases/download/v0.5.0-r1/historical-evidence-manifest.json)维护；仓库[路径索引](historical-evidence-files.md)用于定位。历史文档中的本地证据链接改为索引链接，原验收结论及版本含义不变。归档 JSON 中的路径继续按原目录解释。正常安装、构建、测试及运行无需下载大型证据归档。

## 无损表示与验证边界

冻结快照只共享已经冻结且内容相同的数据块，各发布版本保留自己的有序引用。不能从当前地图或当前规则重新生成旧版本。解码后的历史快照必须与转换前的 15 份内容逐项一致，调用者之间保持数据隔离。

世界路线使用冻结的命令及重复段表示；重放前恢复原始步骤。每个时间戳、命令、预期结果、检查点及路线元数据必须保持一致，多次 Tick 仍然逐步执行。显式作者命令负责写入，构建和验证只读。

转换前后的数据量如下（按完整文件计数）：

| 文件组            |  原行数 | 当前行数 |    原字节 |  当前字节 |
| ----------------- | ------: | -------: | --------: | --------: |
| 15 份冻结发布快照 | 204,352 |   30,890 | 5,893,073 |   944,587 |
| 五份世界路线 JSON | 298,097 |   32,025 | 6,600,372 | 1,255,883 |

15 份展开快照保留完整属性与数组顺序，并逐份记录转换前的序列化 SHA-256。五份路线包含 24 条主线/全收集/续玩路线，共 38,891 个步骤；独立冻结的完整数据摘要覆盖每个步骤及全部元数据。重复引用解码后独立克隆，调用方修改不能串改其他版本或步骤。

原快照及五份路线原件另存 [frozen-data-originals.zip](https://github.com/LoTwT/camellia-golden-week/releases/download/v0.5.0-r1/frozen-data-originals.zip)，共 486,761 字节，SHA-256 为 `1e7104484e627f4fc32618eff697fc4814b863b1c06192670fd01b8c3308e8b9`。三个新增归档附件已发布并实际重新下载，逐一 SHA-256 相同。

冻结快照转换器 `scripts/compact-frozen-releases.ts` 只接受原始冻结 JSON 和另一个显式输出路径；路线格式及作者入口见 [路线存储说明](../../src/content/witnesses/README.md)。构建、校验与测试不执行作者写入，也不刷新独立摘要。

## 本轮实际验证

冻结安装、712 项原生测试、lint、格式、类型、架构、内容、资源与构建检查已通过；完整 `pnpm run verify` 已退出 0，浏览器验证全部通过。首轮在字体覆盖检查发现新增内部错误字符串含未收录的“块”字，改为内部英文诊断后资源检查通过，再执行完整验证；没有修改字体或降低检查。随后完整浏览器验证在双键松开用例失败：断言把真实 keyup 前的异步快照当作停止基线，宿主消息间隔内仍按住的右键可以合法再移动。三次正常键盘探针只延迟下一条宿主 keyup 180ms，均证明额外移动早于真实松键；改为在 keyup 事件内同步采样位置后，同样三次及零额外延迟对照全部通过。200ms 停步检查、其他断言、输入动作和游戏代码不变。失败记录、红绿探针及正常对照均保留。最终完整验证通过，287 个源码、资源、脚本和测试输入前后 SHA-256 相同。

当前构建的 93 个文件共 6,542,361 字节，与原发布 ZIP 内文件逐一比较完全相同。Vite 在构建时展开冻结数据并生成内容模块，因此本轮存储形式变化没有改变最终游戏产物；原发布 ZIP 继续有效。

PR 文本差异、当前文件体积及完整历史克隆体积单独报告。追加提交可缩小 PR 的最终差异，但旧提交中的对象仍然存在，不能据此宣称历史存储已被删除。

完整浏览器复验于 `2026-09-13T06:19:12.795Z` 完成：五 profile 产物与启动、7 个回归、28 个平台边界、8 个输入/实时边界、防火墙标准/减少效果及 47/57/72 Combo 和旧成绩迁移、M5/M1/M2/M3 四条全收集路线、12 个静态房间边界、4 组分期升级及 M3→M4 续玩全部通过。独立副本实际运行路线作者 `--write`，输出紧凑数据与当前文件解析结果完全相同。

本轮采用追加提交更新现有 PR，不重写既有提交。实现提交为 `864647e36f03ea14df1ed656cb1c7a733a2665f3`，以 eruoos 身份按原远端 OID 保护进行快进推送，PR #1 的 base 仍为 main。GitHub 回读为新增 203,436 行、删除 196 行、363 个变更文件；后续仅文档记录会略微增加行数。

## 网络新克隆

从 GitHub 全历史、单分支、无标签克隆，没有本地对象复用、alternates 或 shallow。冻结安装、712 项测试及完整 build 均通过，93 个正式构建文件逐一相同，工作区干净。

| 指标                       |      本轮前 |   实现提交 |
| -------------------------- | ----------: | ---------: |
| PR 新增行数                |   1,375,244 |    203,436 |
| PR 变更文件                |       1,184 |        363 |
| 当前跟踪文件数             |       1,187 |        366 |
| 当前跟踪文件字节           | 122,953,458 | 15,010,010 |
| 完整历史克隆 Git pack 字节 |           — | 75,583,070 |

PR 新增行数减少约 85.2%，当前跟踪文件体积减少约 87.8%。本轮使用追加提交，历史对象没有删除，因此完整历史 pack 约 72.08 MiB。表中当前文件数据以 ba82f90 为基线；本轮没有在改动前重新克隆该提交。上一轮 878de74 的网络克隆 pack 为 75,117,126 字节，仅作历史参考。不能把当前文件减少解释成历史克隆体积同步减少。

## 验证归档

[pr-data-cleanup-verification.zip](https://github.com/LoTwT/camellia-golden-week/releases/download/v0.5.0-r1/pr-data-cleanup-verification.zip) 共 2044028 字节，SHA-256 为 `e56577c5ae29a8e82a39d364883c9d4dec50840ebbb815b1c527a8165234d42e`，已实际重新下载校验。包含本轮成功与失败的结构化结果、完整 verify 日志、287 输入校验、原件对照、新克隆记录及双键采样红绿探针。原始新截图与 tracing 仍保留在本地 ignored 验证目录；本 ZIP 不重复打包全部图片和构建目录。历史 853 份完整原件与此前 R1 的 1266 份原件由各自独立归档保存。

本轮共新增四个附件，均发布到现有 R1 Release 并完成下载 SHA 校验；原发布标签和游戏 ZIP 保持原内容。
