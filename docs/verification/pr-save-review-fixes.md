# PR #1 存档审查修复（2026-09-13）

用户要求修复对 `d22db8e` 的三项审查发现。本轮沿用原实现分支；不改变固定地图、目标、奖励、规则版本、存档 schema 或历史冻结内容。

## 修复与回归

| 问题                                         | 根因与修复                                                                                                                                           | 固定回归                                                                                                                                                            |
| -------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 从侧路首次进入第二迷宫后，房间进度无法保存   | 返回锚点错误地套用了当前玩家必须已访问的约束。仅区分两种位置的访问要求，仍校验固定房间锚点、世界空间、已发现、安全地板、覆盖与门条件；不凭空登记访问 | `tests/room-return-anchor.test.ts`：12 条正常指令进入、移动、重置、写双槽、导入、恢复和负例；十条 M1–M5 主线/全收集路线的每个稳定事务均验证载荷与恢复，包含完成回访 |
| 有效 JSON 备份重排对象键后被拒绝             | 复用既有迭代 JSON 语义比较，修复捕获/盗取基线、迁移完成布局来源匹配和历史归档冲突。数组顺序、真实值差异和未知字段仍严格校验                          | `tests/save-layout-order.test.ts`：7 个活动房间、14 个非法布局负例、2 个迁移同形反例，共 23 项                                                                      |
| 旧档因布局不兼容退回安全入口，却没有具体提示 | 存储读取和导入准备传递迁移器的原始说明；继续、备份恢复、导入确认统一展示迁移原因、保留的权益及重新体验说明。成功保存后不重复旧提示                   | `tests/migration-notes.test.ts`：实际冻结旧 M5、正式迁移注册表与当前 M5，4 项回归；浏览器模块覆盖三个入口及导入写失败保护                                           |

正常侧路复现为：新档右×3 → R 增幅 → 右×3 → F 进入 A → 上×3 → 右。固定返回点 `a.t.1.-2` 已发现但未访问，玩家进入 `a.maze.02`。修复前入口与活动保存失败、重置无效，退出实际访问返回点后才能保存。修复后活动尝试和完成回访均允许这一合法锚点，当前世界玩家仍必须已访问其所在格。

红绿结果均实际执行：锚点组原版 4 失败 / 8 通过，修复后 12/12；JSON 组原版 9 失败 / 14 通过，修复后 23/23；迁移提示组原版 4 失败，修复后 4/4（包含当前档及成功迁移后不重复提示）。没有为了通过而改变地图、玩法或原始历史夹具。

同形检查覆盖所有当前用户布局对象比较；固定注册内容定义、冻结旧规则、有序数组、内部渲染缓存和原文 SHA 校验保留其原有语义。全部 9 个实时房间的物理入口等于返回点，开始挑战前已经实际站在该格，实时恢复继续使用严格的当前玩家位置校验。

## 实际验证与边界

命令、执行时间与浏览器结果见[本轮结果摘要](evidence/pr-save-review-fixes.json)。

- 在独立源码副本中执行 `pnpm test`：787/787 通过；`pnpm run build`（含 lint、格式、类型、架构、内容与资源检查）、`validate:artifacts` 通过。
- 独立 Chrome 153 定向回归通过：菜单纯键盘、8 个故障/接线回归（含侧路正常输入保存、刷新、导入）、3 个迁移提示入口与导入写失败保护、防火墙标准/减少效果及三档 47/57/72 Combo。截图确认迁移说明可见。
- 本轮不将定向浏览器验证称为完整 `test:browser` / `verify`；此前完整五 profile、全收集和性能证据保持其原执行时点。本轮没有重新安装依赖、网络新克隆、发布 Release 或移动标签。
- 306 个工程输入在验证前后 SHA 不变；本地重建的 93 文件 / 6,542,590 字节与验证副本相同，固定 5174 的 94 次 HTTP 请求全部 200 且字节一致。修复版本机 ZIP 为 5,061,710 字节，SHA-256 `8918a1897fced4c61ecba72112214a9e7bff102b7ee7bebe12a2a88c04e096fa`。
- 原 Release ZIP 是历史 R1 产物，不含这三项修复。最新源码从当前 PR 构建，静态交付状态见[交付记录](release-candidate.md)。

浏览器中的正常侧路回归只用真实键鼠、UI 导出/导入和刷新。迁移提示边界使用基于真实历史导出的合法合成 completedVisit，并仅在独立 context 中注入存储故障，不作为正常通关证据。用户日常浏览器页面和存档不被访问。

本机完整日志、构建及浏览器原始证据保存在 `/Users/caoyujie/codex-artifacts/camellia-pr-fixes-20260913/`，不向仓库重新加入大型原始证据。回归可通过上述测试文件及 `pnpm run test:browser --review-only` 重现。
