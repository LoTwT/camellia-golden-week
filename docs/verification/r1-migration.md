# R1 版本迁移实施记录

本记录对应 [内容合同 §5.1](../spec/content.md#51-版本化完成布局与回访) 与 [工程合同 §5](../spec/architecture.md#5-版本与失败恢复)，由 R1 Goal 的迁移子任务维护。历史通过不能作为 R1 游戏验收。

## 已冻结来源

实施前基线 `7fe7d8e9179e0c9dcef505f454232a9a38321234`；从该提交实际代码装配 M1–M5 的 v1 / v2 / v3 共 15 个完整内容视图。冻结文件及 SHA-256、实际既有浏览器存档及 SHA-256 由 `src/content/history/pre-r1/provenance.json` 维护。没有读取或改写用户日常浏览器存储。

- `src/content/history/frozen-releases.ts` 提供按 profile 裁剪的历史视图；它不从当前地图重建旧定义。
- `pre-r1/static-puzzle.ts` 与 `pre-r1/realtime.ts` 是旧提交原字节规则；旧完成布局、活动布局和旧回访占格调用独立旧静态校验。另冻结 engine / types / progress / clock 与内容 validate，以保留历史测试的完整规则校验；validate 仅改导入相对路径，原字节与转换后摘要分别登记。历史移动复放不会调用 R1 盗取规则。
- 七个实际来源覆盖五个 profile（M1/M2 schema1、M3/M4/M5 schema2）以及 M5 v3 旧全收集迁移和 v3 防火墙正常完成；来源路径、版本和摘要在 manifest 中，不重复复制原始证据文件。

## 当前接口与决定

schema 升至 3，规则升至 4，M1–M5 内容版本为 3 / 4 / 5 / 6 / 6。永久目标继续独立于布局。

`completedRoomLayouts` 保存带内容 / 规则版本的布局，显式兼容映射携带来源和 mappingId；`archivedCompletedRoomLayouts` 保存 roomId / 来源内容版本 / 来源规则版本唯一键以及原布局。当前记录校验对应现规则，旧归档对应冻结原规则；目标没有当前记录也没有合法归档时拒绝。

`r1-room-map.ts` 由主任务维护各旧房间安全入口、成功出口和通行目标。R1 逐房明确归档；旧 `c.routing.01` 原目标保留，不生成新捕获目标。活动尝试或 completedVisit 遇归档映射返回该确定入口，不能使用标准解补造新版布局。

兼容记录的来源映射需在迁移登记中存在，且其原归档按该映射转换后必须等于记录。相同版本不同内容、歧义迁移路径、未知来源、归档键重复、非法原布局和未登记安全映射均拒绝。

## 已执行平台边界验证

- `node --test tests/save-store.test.ts`：25 条通过。
- `node --test tests/r1-save-failures.test.ts`：6 条通过。覆盖备份写入失败、新槽拒写、新槽回读不符 / 抛错 / 完整验证失败，以及 store 重建后的迁移重试仍保留最初原槽备份。
- `node --test tests/r1-migration.test.ts`：7 条通过。验证冻结字节、实际 v3 全收集载荷逐布局归档、永久集合与成绩不变、旧完成回访安全退场、缺失映射 / 非法来源证明 / 重复归档拒绝，以及将 archive 登记伪装成 compatible 来源的反例。另以五份真实 profile 存档验证全部 15 条同级 / 前向升级组合，实际永久账本、成绩与各 profile 分母保留，重复迁移保持同值。
- 四组平台测试合并运行共 71 条通过，原始输出见 [platform-tests.log](r1-evidence-files.md#e-8c24c52493e26c07)。
- 历史 `tests/migrations.test.ts` 已对齐 schema3 包装及保留归档语义，33 条通过；原浏览器导出逐字未改。

这些测试是平台边界验证。后续[平台浏览器专项](r1-platform-browser.md)已通过正常键鼠、导入候选世界切换保护和产品失败提示，共28场景；下文旧完成访问连续链另有实际来源与操作证据。

## 写入失败阶段

`SaveWriteResult` 失败新增 `phase`（beforeWrite / writeRejected / afterWrite）与 `migrationBackupVerified`。写后失败仅保证另一有效槽与验证备份保留；不声称目标槽未改变，不标保存成功。迁移备份记录 intendedWriteSha256 与原槽摘要，重试识别相同候选已经写入时复核并保留原备份；新代数仍来自实际槽检查。普通行动不会覆盖该备份。

## R1 真实命令接线验证

`src/content/witnesses/r1-world.json` 冻结五个 profile 各一条主路径和全收集路线。`scripts/author-r1-witnesses.ts --write` 是唯一显式写入入口：先用冻结旧引擎标记原路线的局部场景段，沿用原世界导航，再通过当前 `dispatch` 执行新的固定静态解和正常计时实时见证。C 区额外捕获通过正常外层移动与 F 进入。它不直接修改玩家位置、分数、目标或门。验证、测试和构建只读取冻结命令，不运行作者脚本。

`current.ts` 只查找已冻结 R1 路线；原 v1 JSON 与 v2 适配器不变，原 current 的 v3 语义保存在 `v3.ts`，对应当时固定实时 JSON。原 world / world-m2 / world-m3 / world-m4 / world-ghost 测试改用完整旧引擎、旧地图及旧校验，135 条独立历史通过，输出见 [historical-world-tests.log](r1-evidence-files.md#e-8cdf0f024f626606)，不算作 R1 的通过数。

当前 R1 `world-r1.test.ts` 的 16 条检查涵盖：

- 十条当前正常命令路线、26 / 51 / 81 / 130 / 130 完整物资账本、每个当前静态机关的真实完成记录，C 区四捕获和三盗取全部完成；主路径不打计分挑战。
- 四个相邻 profile 在真实路线共同检查点保存，按已登记兼容映射迁移当前布局，再通过冻结后续命令续玩至该版本全收集。M4→M5 没有新增游戏内容，完整成果原样续用。
- 真实既有 M5 v3 全收集存档的九个旧布局归档，正常传送和步行进入新版盗取；退出、刷新不生成记录，首次成功才补存当前布局，第二次练习保留第一份，旧归档、旧目标和物资账本始终不变。
- 真实旧迷宫完成后可以自然走过原入口至出口；主动体验新迷宫并踩到真实危险格，失败恢复预览且不生成新版完成布局。

`completed-rooms.test.ts` 与 `save-payload.test.ts` 已改为当前 R1 地图；全部十二个静态定义都经真实首次成功、完成态回访、合法占格与严格保存校验。保存成绩现在按具体挑战定义的规则版本解析，R1 全局 rule4 下沿用 rule3 的防火墙无需历史 registry 也能正常保存。

`session-progress.test.ts` 与 `upgrade-summary.test.ts` 将原始浏览器字节经当前正式迁移再恢复；显式重读、迁移备份、失锁旧副本保护、代数更新和同 profile 不误报新增内容均通过。v2/v3 防火墙旧世界输入用独立历史引擎重放，其升级检查仍指向实际 R1 目标，保留原成绩与账本并验证新归档步骤。

合并平台、旧版本成绩升级、当前回访/存档/会话与 R1 世界检查：173 条通过、0 失败，见 [integrated-platform-world-tests.log](r1-evidence-files.md#e-09ad00d62512d1e2)。当前全项目 TypeScript 检查通过，所负责文件 Oxlint 与格式检查通过。`prepare-acceptance-fixtures.ts --check` 使用完整冻结 v1 运行时复核原故障夹具，10 个夹具和 9 个双槽组合均符合原 manifest，历史来源字节未变；这部分仍明确属于历史证据。

## C 捕获完整状态空间补证

主任务追加的有界验证由 `scripts/author-r1-capture-analysis.ts` 完成。显式 `--write` 仅生成 `evidence/r1/capture-state-space.json`；`--check` 重新完整枚举并与冻结证据逐字段比较，全程只读。该脚本不修改地图或见证，也不由构建自动执行。

节点身份包括玩家、每辆具名推车、每个未捕获邦布的位置及排序后的已捕获 ID；边只使用实际 `moveCapture` 的合法四向动作。按实际局部规则，成功即终止；撤销与重置不属于状态图边。宽度优先搜索穷尽全部初态可达节点，所有成功终点反向搜索得到可解节点集合。证据记录规则文件摘要、各定义摘要、完整图摘要、边数与节点分类、具体最短成功和首个死角输入及其完整占格。

| 固定关卡     | 全部可达节点 | 可到达成功 | 不可解节点 |
| ------------ | -----------: | ---------: | ---------: |
| c.capture.01 |          872 |        494 |        378 |
| c.capture.02 |         1636 |        892 |        744 |
| c.capture.03 |          308 |        207 |        101 |
| c.capture.04 |          308 |        207 |        101 |

四关均存在不可解死角，因此保留 `deadEndRecovery` 类型并替换之前未证明死角的恢复路线。每条现在真实走入所记录死角、撤销最后一步回到可解前态、重置后重放原成功动作。现有 success 与 alternative 见证未修改。

`capture-state-space.test.ts` 通过当前 `moveStatic` 到达四个具体死角，并以独立前向搜索确认从该节点没有成功路径、撤销前态有成功路径；验证重置完整恢复、最后正常成功以及真实 blocked 动作无状态变化。状态空间、C 区规则与 R1 世界合并 56 条通过，见 [capture-state-space-tests.log](r1-evidence-files.md#e-adcf950fdeaf8ee8)。TypeScript、新增文件 Oxlint 与格式检查通过。本结论仅针对本版四张固定地图，不外推到未知原版规则。

## P08 真实旧完成访问 → R1 正常 UI 连续链

2026-09-12 使用本机独立 Chrome 153.0.8010.36，在隔离 context、动态回环端口和独立构建目录执行。没有访问日常 5174、注入游戏状态或写入浏览器存储。`scripts/browser/r1-layout-migration.ts` 导出历史来源作者 helper 与当前验证 helper，并提供独立 CLI；所有游戏变更均经真实按钮、file chooser、方向键 / F、导出文本和重新载入的“继续游戏”。只读 inspection 用于核对实际位置、阶段和布局。

先核验 `7fe7d8e9179e0c9dcef505f454232a9a38321234` 确为 schema2 / global rule3，使用 `git archive` 提取独立临时目录并连接固定 node_modules。旧构建使用 `test-results/r1-old-layout-source-build`，当前构建使用 `test-results/r1-layout-migration-build`，均不覆盖主交付构建。

### 两份真实旧来源

- **九布局全收集完成访问**：旧 UI 导入既有 v3 全收集字节（SHA-256 `875124f82484520f3bc84c90e25dca69c3f5883b1184e923f66144333cdfcefb`），通过区域图传送 C、↑↑←←、F 进入旧盗取01完成访问，再 UI 导出。来源位于旧 `c.theft.01.t.1.5`、`room.status=completedVisit`，九个布局及全部原永久集合不变。新导出 SHA-256 `60e51c41167a26eadc6f57926d939abb4b3c08c5c756c0859ce95a20171a9c78`；[来源与实际输入](r1-evidence-files.md#e-d40c6a2ee0c1e51c)、[原始导出](r1-evidence-files.md#e-cabea37a4fd68b95)。
- **六布局且盗取物资尚未领取**：旧 UI 导入真实 M2 浏览器字节（SHA-256 `b8e6fcaac082820270ccc4c19c214d6f1f98d786426b9904a037aefcfc13fe18`），正常走 B4.0→B8.0/F 到 C，再↑↑←←/F进入旧盗取01，按冻结原10步见证实际成功。未进入北侧奖励格，再 F 完成访问并导出。此时只有原11个奖励，旧盗取目标已完成，`c.supply.theft01` 未领取，六个完成布局全部来自实际旧成果。新导出 SHA-256 `5e7e575d78d5058fade4997d93099a85570bd6485d7dc4e02db6edf9ab4c2a31`；[来源与实际输入](r1-evidence-files.md#e-5910652db48fe926)、[原始导出](r1-evidence-files.md#e-a3d5eb19b9af0918)。来源说明文字曾按已记录步骤补全；原始导出字节、摘要及动作观察未改。

### 当前正常链已验证

两份来源均经 R1 UI 导入预览、确认替换，旧完成访问按具名映射退到安全 `c.t.-2.-2`，当前布局为空，原布局逐项进入归档。随后正常传送 A 并走过旧迷宫入口，F 新版练习实际踩危险格失败、导出仍无当前布局，正常放弃；再正常传送 C、进入盗取新版练习，第一处实际组合后第28步导出并刷新，当前尝试完整恢复、撤销历史清空但无新版完成记录。放弃仍无记录；重新正常成功才补存首份当前布局；刷新后普通 F 回访该布局，再 F 进入独立练习并走不同组件推动顺序的合法 alternative，导出与再次刷新均保留首份记录、原归档及原奖励集合。

盗取01的不同合法路线具有不同动作轨迹和最终玩家位置；固定彩色接口决定相同的最终组件布局。它验证正常替代过程与保存稳定性，不冒称不同持久组件终局。不同最终布局不覆盖首份的规则验证另由 capture02 测试提供。

未领奖链额外在刚迁移、尚未补玩新机关时正常走过旧盗取奖励门，确认无需新版完成布局即可通行；没有提前领取。连续链后再正常走到原物资格，仅新增一次 `c.supply.theft01`（5单位），返回再踏格与刷新不重复发放，旧目标、六个归档及首份新布局不变。

通过结果：[九布局完整链](r1-evidence-files.md#e-4c51624d458b2ff5)、[六布局未领奖链](r1-evidence-files.md#e-614d41b04f1da764)。各目录保存导入预览、每阶段 UI 导出、实际动作观察、最终截图和浏览器 trace。

原始失败保留：第一次旧新游戏初始化发生 clockGap，脚本后续仅在首次游戏输入前观察到可见调度暂停时点击正常“继续探索”；第一次 R1 中途放弃时验收控制面板遮住底部按钮，改经正常暂停菜单的“放弃本次尝试，返回入口”完成，没有强制点击或隐藏面板。失败 JSON、截图和 trace 均未删除；最终通过不能替代这些原失败记录。两次旧构建 / 新构建的 Vite 650kB 提醒属于构建体积警告，未调整阈值。
